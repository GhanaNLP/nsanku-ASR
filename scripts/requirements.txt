# nsanku-ASR — H200 GPU setup
# Install: pip install -r scripts/requirements.txt

# HF model loading + ASR
torch>=2.4.0
transformers>=5.0.0   # 4.x breaks waxal Wav2Vec2 tokenizers (see the qwen-asr note)
accelerate>=1.0.0
datasets>=3.0.0
soundfile>=0.12.0

# Audio processing
torchaudio>=2.4.0

# Metrics
jiwer>=3.0.0

# Gemini API
google-genai>=1.0.0

# Google Speech Recognition track (benchmark/google.py)
SpeechRecognition>=3.10.0

# NOT installed: qwen-asr. It is the only way to load Qwen3-ASR checkpoints
# (saved under a `thinker.` prefix that transformers cannot read), but it pins
# transformers to 4.x, and 4.57 rejects the tokenizer config of every
# waxal-benchmarking Wav2Vec2/MMS model with
# "'list' object has no attribute 'keys'". Install it in a separate venv if you
# need it; see SKIP_MODELS in benchmark/config.py.

# Data formats
pyyaml>=6.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lz4>=4.3.0

# HF hub
huggingface_hub>=0.26.0
